userhost-in-names Client Capability Extension
---------------------------------------------

When requested, this capability will cause the IRC server to send
users' full hosts in the NAMES reply.

For example:

    :irc.example.org 353 Nick = #channel :+Nick2!user2@host.example.org
